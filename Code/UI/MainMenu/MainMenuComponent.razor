@namespace Facepunch.Minigolf.UI
@inherits PanelComponent
@implements IGameRootPanel

<root>
    <MainMenu />
</root>

@code{
	protected override void OnStart()
	{
		base.OnStart();
		if (Game.IsRunningInVR)
		{
			GameObject.Clone("prefabs/vrplayer.prefab");
		}
	}
}
